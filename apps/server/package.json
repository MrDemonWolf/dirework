{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy src/index.ts --dry-run --name dirework-api --compatibility-date 2026-07-30 --compatibility-flag nodejs_compat --outdir dist",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@dirework/api": "workspace:*",
    "@dirework/auth": "workspace:*",
    "@dirework/db": "workspace:*",
    "@dirework/env": "workspace:*",
    "@hono/trpc-server": "^0.4.0",
    "@trpc/server": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@dirework/config": "workspace:*",
    "@types/node": "^26.1.2",
    "alchemy": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
